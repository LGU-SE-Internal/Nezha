# Nezha 优化版本使用说明

## 概述

根据 `todo.md` 中的优化方案，我们实现了 Nezha 的高性能版本，将原本需要"天"级别的处理时间优化到"分钟"级别。

## 优化核心思想

### 问题分析
原始版本的性能瓶颈：
1. 为每个正常Trace构建并保留独立的EventGraph对象（16万个图对象）
2. 将所有图对象全部加载到内存中
3. 在诊断时反复遍历16万个图来计算最大深度

### 解决方案
我们将工作流程分为两个阶段：

#### 第一阶段：离线预计算（处理"正常"数据）
- **目标**：生成包含后续诊断所需全部信息的轻量级"模式画像"文件
- **方法**：
  1. 初始化全局"模式画像"字典 `global_pattern_profile`
  2. 并行处理所有正常Trace，在内存中临时构建事件图并立即计算深度
  3. 子进程只返回轻量级的模式深度信息，不返回庞大的图对象
  4. 聚合所有结果，更新全局模式画像
  5. 将结果缓存到磁盘文件

#### 第二阶段：在线诊断（处理"故障"数据）
- **目标**：以最快速度完成故障诊断
- **方法**：
  1. 从磁盘快速加载模式画像缓存
  2. 采用极致优化的"直接计数"法统计故障频率
  3. 并行处理，完全不构建图对象，直接解析边列表进行频率计数
  4. 执行诊断逻辑，无需遍历或重复计算

## 性能收益

- **深度计算复杂度**：从 O(N*M) 降为 O(1) 的字典查找
- **内存占用**：大幅降低，不再需要保留16万个图对象
- **故障诊断**：消除最耗时的图构建和聚合步骤

## 使用方法

### 1. 使用优化版本
```bash
# 运行优化版本
python main.py --optimized --ns hipster --level service

# 或者使用内部服务级别
python main.py --optimized --ns hipster --level inner-service
```

### 2. 使用原始版本（对比）
```bash
# 运行原始版本
python main.py --ns hipster --level service
```

### 3. 命令行参数说明
- `--optimized`: 启用优化版本（实现todo.md的优化方案）
- `--ns`: 命名空间，可选 "hipster" 或 "ts"
- `--level`: 分析级别，可选 "service" 或 "inner-service"

## 文件结构

```
Nezha/
├── main.py                     # 主程序入口（已修改，支持优化版本）
├── optimized_pattern_ranker.py # 优化版本实现
├── pattern_ranker.py          # 原始版本（已添加优化函数）
├── cache/                     # 缓存目录
│   └── pattern_profile_*.pkl  # 模式画像缓存文件
└── todo.md                    # 原始优化方案文档
```

## 缓存机制

- 第一次运行时会生成模式画像缓存文件
- 后续运行会自动使用缓存，大幅提升启动速度
- 缓存文件位于 `cache/pattern_profile_{namespace}.pkl`
- 如需重新生成缓存，删除对应缓存文件即可

## 性能对比

### 原始版本
- 离线处理：数小时到数天
- 在线诊断：数分钟到数小时
- 内存占用：极高（16万个图对象）

### 优化版本
- 离线处理：数分钟到数小时（仅需运行一次）
- 在线诊断：数秒到数分钟
- 内存占用：大幅降低（轻量级缓存）

## 注意事项

1. **首次运行**：优化版本首次运行时需要构建缓存，可能需要一些时间
2. **缓存管理**：如果数据发生变化，需要删除缓存文件重新生成
3. **兼容性**：优化版本与原始版本输出格式完全兼容
4. **错误处理**：如果优化版本不可用，会自动回退到原始版本

## 实现细节

优化版本主要在以下文件中实现：
- `optimized_pattern_ranker.py`: 核心优化算法
- `main.py`: 集成优化版本的启动逻辑

关键优化函数：
- `build_pattern_profile_cache()`: 构建模式画像缓存
- `fast_online_diagnosis()`: 快速在线诊断
- `create_optimized_evaluation_functions()`: 创建优化版本的评估函数
